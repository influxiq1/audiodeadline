import { CompuserfilterPipe } from './compuserfilter.pipe';

describe('CompuserfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompuserfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
