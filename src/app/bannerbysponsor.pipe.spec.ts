import { BannerbysponsorPipe } from './bannerbysponsor.pipe';

describe('BannerbysponsorPipe', () => {
  it('create an instance', () => {
    const pipe = new BannerbysponsorPipe();
    expect(pipe).toBeTruthy();
  });
});
