import { BannerfilterPipe } from './bannerfilter.pipe';

describe('BannerfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BannerfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
