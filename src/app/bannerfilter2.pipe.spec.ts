import { Bannerfilter2Pipe } from './bannerfilter2.pipe';

describe('Bannerfilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Bannerfilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
